[tool.poetry]
name = "django-quill-editor"
version = "0.2.0"
description = ""
authors = ["이한영 <dev@lhy.kr>", "michaldyczko", "Jonhnatha Trigueiro <joepreludian@gmail.com>"]
packages = [
    { include = "django_quill" },
]

[tool.poetry.dependencies]
python = ">3.6,<4"
django = "^3.2"

[tool.poetry.dev-dependencies]
Sphinx = "^3.1"
recommonmark = "^0.6"
sphinx-rtd-theme = "^0.5"

[build-system]
requires = ["poetry>=0.12", "setuptools>=30.3.0,<50"]
build-backend = "poetry.masonry.api"
